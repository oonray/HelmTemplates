{{ define "labels" }}
app: "{{ .Chart.Name }}"
env: "{{ .Values.environment }}"
{{ end }}

{{ define "labels.extra" }}
{{ range $lbl := .labels }}
{{ $lbl.key }}: {{ $lbl.value | quote}}
{{ end }}
{{ end }}
